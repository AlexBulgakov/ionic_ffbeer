"use strict";
function generator(options) {
    var generate = options.generate || defaultGenerate;
    return function (api, data) {
        var files = generate(options.templates, api, data);
        return { files: files, api: api, data: data, options: options };
    };
}
exports.generator = generator;
function defaultGenerate(templates, api, data) {
    var files = {};
    Object.keys(templates).forEach(function (key) {
        files[key] = templates[key](api, data);
    });
    return files;
}
//# sourceMappingURL=index.js.map