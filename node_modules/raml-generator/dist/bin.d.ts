import Promise = require('any-promise');
import { Generator } from './index';
export interface Pkg {
    name: string;
    description: string;
    version: string;
}
export declare function bin(generator: Generator, pkg: Pkg, argv: string[]): Promise<void>;
